# If you have OpenSSL installed, we recommend updating
# the following line to use "https"
source 'http://rubygems.org'

# heroku wants the ruby version set here in the Gemfile
ruby '2.0.0'
gem "compass"
gem "breakpoint"
